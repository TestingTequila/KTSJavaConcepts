Whenever we hava to call any variables, methods from a class, we have only tow ways to do so

1. if variables and methods are non-static=> call them using class object
2. if variables and methods are static    => call them using class name

